{
  "name": "rapid-fs-class",
  "description": "Instead of storing the content as a file using the given filename, store the content using the hash of that content.",
  "author": {
    "email": "wolferi93@gmail.com",
    "name": "Ferenc Farkas"
  },
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "compile" : "tsc src/index.ts",
    "run-js" : "node src/index.js",
    "run-ts" : "node --require ts-node/register src/index.ts",
    "deploy-gcp" : "gcloud app deploy gcp.yaml"
  },
  "dependencies": {
    "@types/node": "^20.8.9",
    "crypto": "^1.0.1",
    "fs": "^0.0.1-security",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
